================================================================================
  CANDIDATE PIPELINE UPGRADE - CODE USAGE GUIDE & EXAMPLES
================================================================================

📚 COMPONENT DOCUMENTATION & CODE EXAMPLES
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1️⃣  InterviewDetailsRow.jsx
═════════════════════════════════════════════════════════════════════════════

Import:
──────
import InterviewDetailsRow from './InterviewDetailsRow';

Usage:
──────
<InterviewDetailsRow 
  interview={interview}
  showTag={true}
  tagLabel="Shortlisted"
/>

Props:
──────
- interview: Object           // Interview data from API
- showTag: Boolean           // Show status tag (default: true)
- tagLabel: String          // Custom tag label (default: 'Shortlisted')

Returns:
────────
Displays in this layout:
┌────────────────────────────────────┐
│ Interview Scheduled    [Shortlisted]│
├────────────────────────────────────┤
│ 📅 Jan 25, 2026 │ ⏰ 2:30 PM       │
│ 📍 Online • Conference Room │ 👤 Jane│
└────────────────────────────────────┘

Interview Object Structure:
──────────────────────────
{
  _id: "...",
  scheduledDate: "2026-01-25",
  scheduledTime: "14:30",
  mode: "Online",              // Online, Offline, Telephonic
  location: "Conference Room",
  interviewerId: { name: "Jane Smith" },
  interviewerName: "Jane Smith",
  meetingLink: "https://zoom.us/..."
}


2️⃣  StatusActionRow.jsx
═════════════════════════════════════════════════════════════════════════════

Import:
──────
import StatusActionRow from './StatusActionRow';

Usage:
──────
<StatusActionRow
  candidateId={candidate._id}
  onSelected={handleSelected}
  onRejected={handleRejected}
  onMoveToRound={handleMoveToRound}
  availableRounds={availableRounds}
  loading={loading}
  selectedButtonLoading={selectedButtonLoading}
  disabled={disableActions}
/>

Props:
──────
- candidateId: String              // Candidate ID
- onSelected: Function             // Callback for Selected button
- onRejected: Function             // Callback for Rejected button
- onMoveToRound: Function          // Callback for Move button
- availableRounds: Array           // List of available rounds
- loading: Boolean                 // Overall loading state
- selectedButtonLoading: String    // Which button is loading ('selected', 'rejected', 'moveToRound')
- disabled: Boolean                // Disable all buttons

Returns:
────────
Displays three action buttons:
┌──────────────────────────────────────────────────┐
│ Next Step Actions                                │
├──────────────────────────────────────────────────┤
│ [✅ Selected] [❌ Rejected] [➡️ Move to Round]  │
│                                                  │
│ Select an action to update the pipeline status   │
└──────────────────────────────────────────────────┘

Handler Implementation:
──────────────────────
const handleSelected = async (candidateId) => {
  setSelectedButtonLoading('selected');
  try {
    await api.post(`tracker/candidates/${candidateId}/status`, {
      status: 'Selected',
      stage: 'HR Round',
      remarks: 'Candidate selected and moved to HR Round',
      actionBy: 'HR Manager'
    });
    await loadData();
    alert('✅ Candidate moved to HR Round');
  } catch (err) {
    console.error('Error:', err);
    alert('Failed to update candidate status');
  } finally {
    setSelectedButtonLoading('');
  }
};


3️⃣  MoveToRoundDropdown.jsx
═════════════════════════════════════════════════════════════════════════════

Import:
──────
import MoveToRoundDropdown from './MoveToRoundDropdown';

Usage:
──────
<MoveToRoundDropdown
  availableRounds={availableRounds}
  onSelect={handleMoveRound}
  disabled={false}
  loading={selectedButtonLoading === 'moveToRound'}
/>

Props:
──────
- availableRounds: Array    // List of round objects
- onSelect: Function        // Callback when round selected
- disabled: Boolean         // Disable dropdown
- loading: Boolean          // Show loading spinner

Round Object Format:
────────────────────
{
  id: 'hr-round',           // Unique identifier
  label: 'HR Round'         // Display label
}

Example Rounds:
───────────────
[
  { id: 'hr', label: 'HR Round' },
  { id: 'technical', label: 'Technical Round' },
  { id: 'final', label: 'Final Round' }
]

Handler Implementation:
──────────────────────
const handleMoveRound = (round) => {
  setSelectedButtonLoading('moveToRound');
  // Call API to move candidate
  api.post(`tracker/candidates/${candidate._id}/status`, {
    status: 'Interview Scheduled',
    stage: round.label,
    remarks: `Moved to ${round.label}`,
    actionBy: 'HR Manager'
  });
};

Returns:
────────
Dropdown button with optional dropdown menu:
┌──────────────────────────────────────────┐
│ [Move to Round ▼]                        │
│ ┌──────────────────────────────────────┐ │
│ │ • HR Round                           │ │
│ │ • Technical Round                    │ │
│ │ • Final Round                        │ │
│ └──────────────────────────────────────┘ │
└──────────────────────────────────────────┘


4️⃣  CandidateCard.jsx
═════════════════════════════════════════════════════════════════════════════

Import:
──────
import CandidateCard from './CandidateCard';

Usage Examples:
───────────────

Example 1: Shortlisted tab (with interview)
─────────────────────────────────────────────
<CandidateCard
  candidate={candidate}
  interview={interview}
  showInterviewDetails={!!interview}
  showActionButtons={false}
/>

Example 2: Interview tab (with action buttons)
───────────────────────────────────────────────
<CandidateCard
  candidate={candidate}
  interview={interview}
  showInterviewDetails={!!interview}
  showActionButtons={!!interview}
  onSelected={handleSelected}
  onRejected={handleRejected}
  onMoveToRound={handleMoveToRound}
  availableRounds={availableRounds}
  selectedButtonLoading={selectedButtonLoading}
  disableActions={selectedButtonLoading !== ''}
/>

Props:
──────
- candidate: Object              // Candidate data
- interview: Object              // Interview data (optional)
- showInterviewDetails: Boolean  // Show interview section
- showActionButtons: Boolean     // Show action buttons
- availableRounds: Array         // For dropdown
- onSelected: Function           // Called on Selected button
- onRejected: Function           // Called on Rejected button
- onMoveToRound: Function        // Called on Move button
- loading: Boolean               // Loading state
- selectedButtonLoading: String  // Which button is loading
- disableActions: Boolean        // Disable all actions

Returns:
────────
Conditional layout based on props:

Layout 1 (Resume Only):
┌────────────────────────────┐
│ 📄 Resume & CV            │
│ 📋 Candidate's materials  │ [Download]
└────────────────────────────┘

Layout 2 (Resume + Interview):
┌────────────────────────────┐
│ 📄 Resume & CV            │
│ [Download]                │
└────────────────────────────┘
┌────────────────────────────┐
│ Interview Details...      │
└────────────────────────────┘

Layout 3 (Resume + Interview + Actions):
┌────────────────────────────┐
│ 📄 Resume & CV            │
│ [Download]                │
└────────────────────────────┘
┌────────────────────────────┐
│ Interview Details...      │
└────────────────────────────┘
┌────────────────────────────┐
│ [✅ Selected]            │
│ [❌ Rejected]            │
│ [➡️ Move to Round ▼]     │
└────────────────────────────┘

Candidate Object Structure:
───────────────────────────
{
  _id: "...",
  name: "John Doe",
  email: "john@example.com",
  phone: "1234567890",
  requirementTitle: "Senior Developer",
  resumeUrl: "https://...",
  currentStatus: "Interview Scheduled",
  appliedOn: "2026-01-15",
  updatedAt: "2026-01-22"
}


5️⃣  CandidateTimeline.jsx (Updated)
═════════════════════════════════════════════════════════════════════════════

New Features:
─────────────

Tab Navigation State:
const [activeTab, setActiveTab] = useState('timeline');

Tab Rendering Functions:
────────────────────────
- renderShortlistTab()     // Shows if status === 'Shortlisted'
- renderInterviewTab()     // Shows if status === 'Interview Scheduled'
- renderHRRoundTab()       // Shows if status === 'Selected'
- renderRejectedTab()      // Shows if status === 'Rejected'
- renderTimeline()         // Always available

Handler Functions:
──────────────────
handleSelected(candidateId)
├─ Updates status to 'Selected'
├─ Updates stage to 'HR Round'
├─ Calls tracker API
└─ Reloads candidate data

handleRejected(candidateId)
├─ Updates status to 'Rejected'
├─ Updates stage to 'Final'
├─ Calls tracker API
└─ Reloads candidate data

handleMoveToRound(round)
├─ Updates status to 'Interview Scheduled'
├─ Updates stage to round.label
├─ Calls tracker API with round info
└─ Reloads candidate data

Tab Visibility Logic:
─────────────────────
Shortlisted Tab  → Show if: status === 'Shortlisted' OR status === 'Interview Scheduled'
Interview Tab    → Show if: status === 'Interview Scheduled'
HR Round Tab     → Show if: status === 'Selected'
Rejected Tab     → Show if: status === 'Rejected'
Timeline Tab     → Always show

Component Integration:
──────────────────────

In renderShortlistTab():
  └─ <CandidateCard
       showInterviewDetails={!!interview}
       showActionButtons={false}
     />

In renderInterviewTab():
  └─ <CandidateCard
       showInterviewDetails={!!interview}
       showActionButtons={!!interview}
       onSelected={handleSelected}
       onRejected={handleRejected}
       onMoveToRound={handleMoveToRound}
     />

In renderHRRoundTab():
  ├─ Status badge "Selected for HR Round"
  ├─ "Selected — Processed on {date}"
  └─ <CandidateCard
       showInterviewDetails={false}
       showActionButtons={false}
     />

In renderRejectedTab():
  ├─ Status badge "Application Closed"
  ├─ "Rejected — Processed on {date}"
  └─ <CandidateCard
       showInterviewDetails={false}
       showActionButtons={false}
     />

================================================================================

API INTEGRATION EXAMPLES
════════════════════════════════════════════════════════════════════════════

1. Update Candidate Status (Move to HR Round):
──────────────────────────────────────────────
const response = await api.post(`tracker/candidates/${id}/status`, {
  status: 'Selected',
  stage: 'HR Round',
  remarks: 'Candidate selected after successful interview',
  actionBy: 'HR Manager'
});

Response: { success: true, message: "Status updated" }

2. Update Candidate Status (Reject):
─────────────────────────────────────
const response = await api.post(`tracker/candidates/${id}/status`, {
  status: 'Rejected',
  stage: 'Final',
  remarks: 'Candidate did not meet technical requirements',
  actionBy: 'HR Manager'
});

3. Move to Another Round:
─────────────────────────
const response = await api.post(`tracker/candidates/${id}/status`, {
  status: 'Interview Scheduled',
  stage: 'Technical Round',  // From dropdown
  remarks: 'Moved to Technical Round from HR Round',
  actionBy: 'HR Manager'
});

4. Fetch Interview Data:
────────────────────────
const interview = await api.get(`/hrms/interviews/${candidateId}`);
// Response: { _id, scheduledDate, scheduledTime, ... }

5. Fetch Candidate Data:
────────────────────────
const candidate = await api.get(`tracker/candidates/${candidateId}`);
// Response: { _id, name, email, phone, ... }

6. Fetch Timeline:
──────────────────
const timeline = await api.get(`tracker/candidates/${candidateId}/timeline`);
// Response: Array of timeline logs

================================================================================

STATE MANAGEMENT FLOW
═════════════════════════════════════════════════════════════════════════════

Initial Load:
────────────
1. Load candidate data
2. Load interview data (if exists)
3. Load timeline data
4. Set activeTab to 'timeline'

User Clicks "Selected" Button:
──────────────────────────────
1. setSelectedButtonLoading('selected')
2. Call API: update status to 'Selected'
3. Call loadData() to refresh everything
4. Candidate moves to HR Round tab automatically
5. setSelectedButtonLoading('')

User Moves to Another Round:
────────────────────────────
1. User opens dropdown → Round selected
2. setSelectedButtonLoading('moveToRound')
3. Call API: update status & stage
4. Call loadData() to refresh
5. Candidate moves to selected round tab
6. Show success message
7. setSelectedButtonLoading('')

Tab Switching:
──────────────
1. User clicks tab button
2. setActiveTab(tabId)
3. Render corresponding tab content
4. No API calls (data already loaded)

================================================================================

COMMON PATTERNS & BEST PRACTICES
═════════════════════════════════════════════════════════════════════════════

Pattern 1: Loading States for Multiple Buttons
──────────────────────────────────────────────
Instead of: loading={loading}
Use: selectedButtonLoading={selectedButtonLoading}

This allows individual button loading states while others remain clickable.

Pattern 2: Conditional Rendering Based on Status
─────────────────────────────────────────────────
{activeTab === 'interview' && candidate.currentStatus === 'Interview Scheduled' && (
  <StatusActionRow {...props} />
)}

Pattern 3: Tab Visibility
──────────────────────────
Tab visibility depends on candidate.currentStatus:
- Shortlisted  → status === 'Shortlisted' OR status === 'Interview Scheduled'
- Interview    → status === 'Interview Scheduled'
- HR Round     → status === 'Selected'
- Rejected     → status === 'Rejected'
- Timeline     → Always visible

Pattern 4: Error Handling
─────────────────────────
try {
  // API call
  await api.post(...)
  alert('✅ Success message');
} catch (err) {
  console.error('Error:', err);
  alert('❌ Error message');
} finally {
  setSelectedButtonLoading('');
}

================================================================================

PERFORMANCE CONSIDERATIONS
═════════════════════════════════════════════════════════════════════════════

✓ No unnecessary re-renders
  └─ Each component has specific props and only re-renders when props change

✓ Loading states prevent multiple submissions
  └─ Buttons disabled during API calls

✓ Data loaded once
  └─ Timeline, interview, and candidate data loaded once on component mount

✓ Conditional rendering
  └─ Only visible tabs are rendered, saving memory

✓ Optimized API calls
  └─ Using existing endpoints, no additional queries

================================================================================

TESTING CHECKLIST
═════════════════════════════════════════════════════════════════════════════

[ ] Navigate to candidate detail page
[ ] Verify tabs show based on candidate status
[ ] Click "Selected" button in Interview tab
  [ ] Button shows loading spinner
  [ ] API call succeeds
  [ ] Tab switches to HR Round
  [ ] Status badge updates
[ ] Click "Rejected" button
  [ ] Button shows loading spinner
  [ ] Candidate moves to Rejected tab
  [ ] Status updates to red badge
[ ] Click "Move to Round" dropdown
  [ ] Dropdown opens with round options
  [ ] Selecting a round calls API
  [ ] Candidate moves to new round tab
[ ] Verify interview details display
  [ ] Date, time, location, interviewer show
  [ ] Meeting link visible for online interviews
[ ] Check responsive design
  [ ] Mobile: buttons stack vertically
  [ ] Tablet: 2-column layout
  [ ] Desktop: full-width optimized
[ ] Verify timeline tab still works
  [ ] Shows all previous updates
  [ ] Grouped by stages

================================================================================

TROUBLESHOOTING GUIDE
═════════════════════════════════════════════════════════════════════════════

Issue: Buttons not showing in Interview tab
─────────────────────────────────────────────
Solution: Check candidate.currentStatus === 'Interview Scheduled'
         Check interview data is loaded: if (!interview) return null;

Issue: Interview details not displaying
─────────────────────────────────────────
Solution: Verify interview API returns data
         Check scheduledDate and scheduledTime are present

Issue: Tab not switching after action
──────────────────────────────────────
Solution: Verify loadData() is called after API success
         Check activeTab state is being updated

Issue: Loading spinner stuck
─────────────────────────────
Solution: Check finally block: setSelectedButtonLoading('')
         Verify API promise resolves or rejects

Issue: Dropdown not closing
────────────────────────────
Solution: Check click-outside handler
         Verify isOpen state is toggled correctly

================================================================================
